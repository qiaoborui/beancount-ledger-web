# 20260921-go-bounded-stream-verifier

- Status: completed (Go deliverables); updated 2026-09-21T18:37:06+08:00.
- Goal: independent phase-1 bounded-v1 canonical JSONL verification and summary-only CLI; no SQLite, new dependencies, default runtime change, or financial writes.
- Branch: `codex/ios-bounded-stream`; base code revision: `e074d4792005b16ea6922478d7b43526c364c273`. New files are local-only, uncommitted/unpushed; no PR created by this task. Delegated implementation did not perform commit/push/switch; publication belongs to the parent task. Not ready in another clone.
- Completed files: `server/internal/boundedstream/verify.go`, `server/internal/boundedstream/verify_test.go`, `server/cmd/ledger-stream-check/main.go`. This sanitized handoff is the only additional project artifact, required by project handoff policy. Pre-existing Python exporter/test work was not changed.
- API: `Verify(ctx, reader, visit func(Record) error) (Summary, error)`; raw callback records own their bytes, but all callbacks remain provisional until footer and EOF validate. Failure returns zero summary. Caller must bound retained callback data; cancellation cannot interrupt a blocked arbitrary reader/callback.
- Checks: 1MiB inclusive-LF records, UTF-8/escaped surrogate validity, bounded-depth token decode with recursive duplicate-key rejection, exact finite decimal strings without float conversion, known directive/field mappings, typed depth <=16, safe relative paths, ordering/current-parent/contiguous ordinals, lexicographic metadata keys, incremental raw-line SHA256, footer counts/digest and no trailing bytes. No stream-wide seen-ID/key sets. Source digest format is checked, not authenticated against external files.
- Validation on local new-file state: targeted `go test ./internal/boundedstream ./cmd/ledger-stream-check -count=1`, race tests, `go vet` passed. CLI build/success/failure/privacy smoke tests included. Package statement coverage 93.2%. Python Beancount 3.2.3 interoperability passed for all 12 directive kinds and independently recomputed synthetic source digest. Configurable `LEDGER_STREAM_PYTHON`; absent runtime skips integration. 15-second fuzz session passed (23,423 executions; before final buffered-reader hardening, which has its own regression test). `go build ./cmd/ledger-web` passed with output outside checkout. Full `go test ./...` passed with a minimal subprocess environment; inherited deployment/auth/pool environment initially caused unrelated configuration tests to fail. No hosted integration, private ledger, or device tests run.
- Integration follow-up: metadata sorting now enforced with an 8192-key owner bound; nested documents options and wire path grammar fixed; ordering test now requires successful sorted export. All 53 Python tests and full Go tests passed after fixes.
- Options remain exporter-order string records; no unbounded option-key set is retained. Path validation is lexical and does not access ledger sources. A record bound is not a 1MiB total-heap claim; decode allocations are bounded by one record.
- Next step: parent task publishes the integrated phase1 milestone. No remaining Go verifier implementation work identified.
