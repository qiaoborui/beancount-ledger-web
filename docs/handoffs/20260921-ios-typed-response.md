# 20260921-ios-typed-response

- Status: active (B2 implemented/validated; publication pending).
- Updated: 2026-09-21T17:05:14.262838+08:00.
- Goal/authorization: second focused step of approved first-round optimization: direct typed response decoding, avoiding successful JSONSerialization envelope unpack/re-encode on ordinary reads and mutations. Preserve error priority, strict headers, exact numeric decoding, optional/null behavior, pinned revision, and decode-before-publish rollback.
- Branch: `codex/ios-typed-response`; Graphite parent `codex/ios-validation-only` PR405; base `77ea3cca17afb70fe60c65138a047f7b6b2bcb70`. Work uncommitted/unpushed until publication update. No private ledger access, capacity changes, model handles, retention changes or production operations.
- Implementation: additive LocalLedgerResponse wrapper plus engine protocol response requirement/default adapter for existing result-only engines; embedded engine keeps native envelope; repository directly decodes typed models before publication. Legacy dispatch API remains; bootstrap persisted cache keeps its result-only format and materializes JSON only for that optional cache.
- Review fixes: use single-value decoder container to retain root Decimal/Data/Date strategies; cache materialization is best-effort so representability of an ignored field cannot reject an otherwise usable bootstrap. Final independent static review found no remaining must-fix.
- Changed files: `App/LedgerMobile/Sources/LocalLedgerEngine.swift`, `LocalLedgerRepository.swift`; `App/LedgerMobile/Tests/LocalLedgerEngineTests.swift`, `LocalLedgerRepositoryTests.swift`, `LocalLedgerPresentationCacheTests.swift`; this note.
- Validation: full portable Swift411 tests/15 skipped/0 failures under Xcode; optimized Release apphost build passes with ENABLE_TESTABILITY=YES. Apphost34 tests/1 skip/0 failures cover native precision/diagnostics, offline CRUD, bill-import rollback/dedup/reopen, typed/legacy adapters, pinned revision/cache and malformed-envelope stage rollback. Full synthetic10k repository/preview/confirm/invalid-stage baseline passes. No Go/web implementation changes; no additional Go/web suite run.
- Phase performance: opt-in Release host Swift test uses730,035-byte envelope with10,000 exact-Int64 rows, alternating legacy/direct order,20 samples each after2 warmups. Legacy p50/p95 14.86/17.51ms; direct6.69/11.41ms (~55.0% lower median in this decoding phase). Every decode compared to exact expected rows; success path uses no JSONSerialization object graph/re-encode. Reproduce: `LEDGER_PERF_TYPED_DECODE=1 swift test -c release --package-path App/LedgerMobile --filter LocalLedgerEngineTests` under Xcode.
- End-to-end limit: simulator background/diagnostic load remained highly variable, including unchanged canonical/engine control paths. Full baseline verifies correctness, NOT reliable before/after speedup or nonregression. No iPhone/UI/thermal/peak-memory/soak claim. Bootstrap still performs legacy materialization to persist unlocked presentation; no claim that all JSON copies are removed.
- Failed attempt resolved: benchmark initially missed try inside autoreleasepool closure; corrected before successful Release test. No production operation failed.
- Next: commit/push focused branch; create stacked PR targeting PR405 using gh (Graphite local metadata retained; service auth unavailable); check mergeability/conflicts/CI; delete dedicated simulator and mark completion. B1/B2 finish first-round scope; model-handle100k design and retention remain separate future decisions.
