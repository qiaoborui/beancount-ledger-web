# 20260921-ios-bounded-read-model

- Status: active; updated 2026-09-21T20:24:42.519808+08:00.
- Goal/authorization: user approved starting implementation and requested continued execution. Immutable SQLite-derived mobile read model, canonical semantics, bounded records/pages; no silent snapshot fallback. Existing default stays functional until all compatibility/device gates pass.
- Branch: `codex/ios-readindex-core`, child of `codex/ios-bounded-stream` (PR407). Base revision `0603c1addbae43bbfb4a049820b5fc0e5a99b00b`; this milestone's code accompanies this note. Graphite tracks PR404 -> PR405 -> PR406 -> PR407 -> this branch. No merge requested.
- Completed phase 1: independent Python canonical stream exporter, strict bounded Go verifier and summary-only CLI. Exact typed values, split postings/metadata, count/digest checks, exclusive private spool creation and source mutation detection. Metadata owner limit 8192 checked before sorting. No SQLite dependency/default-app changes yet.
- Files: `App/LedgerMobile/Runtime/ledger_stream.py`, `test_ledger_stream.py`; `server/internal/boundedstream/verify.go`, `verify_test.go`; `server/cmd/ledger-stream-check/main.go`; `docs/ios-bounded-stream.md`. Review found metadata sorting allocation and path interoperability issues; all fixed with regression tests.
- Validation on milestone working tree: Python runtime discovery 53 tests PASS including both-language interoperability, full Go test suite PASS under minimal environment, server build PASS, boundedstream race tests PASS. Reviewer additionally ran vet/fuzz. Synthetic 1k/10k/100k exports verified: 100k has 100002 directives, 200000 postings, 44820464 bytes, largest record 205 bytes. This is streaming evidence, NOT an SQLite/index/page/device or RSS result.
- Limits: canonical AST remains O(N); stream consistency digest does not authenticate source; frozen generation required; verifier callbacks provisional through EOF; caller must bound retained records. POSIX permissions do not establish iOS data-protection behavior. Metadata limits fail explicitly. Source fixtures are synthetic; no private ledger or hosted infrastructure used.
- Publication: phase1 committed/pushed as `0603c1a`, PR https://github.com/qiaoborui/beancount-ledger-web/pull/407 (open, non-draft, mergeable; CI backend/gate passed). Vercel preview failed for both PR407 and its parent; not claimed healthy. Phase2 core prepared for child PR; check live remote before resuming.
- Completed phase2 host foundation: native system SQLite adapter and immutable build/open/page/detail core in `server/internal/readindex/`, documented in `docs/ios-readindex-core.md`. Readonly context-aware verification; exact record storage; revision cursors; 1MiB pages/detail errors. Protected spill is fail-closed, not implemented. Review found integrity spill bypass, missing reopen cancellation and parent-directory sync; fixed with tests. Targeted/race/vet/full backend/build and incremental 100k index/reopen/full-pagination tests passed on Linux, including declared Go1.25.10 (100k test13.37s; not a device benchmark). Apple linkage and actual app publication are not implemented.
- Remaining in order: (1) publish core child PR and inspect checks/conflicts; (2) native binding tests; (3) native packaging, protected spool bridge, coherent Swift source/index publication, opt-in bounded UI with explicit unsupported capabilities and Widget isolation; (4) all default reports/BQL/editor/import/Widget bounded migration and parity; (5) failure/lock/device scale gates before default switch.
- Blockers: this host has no Swift/Xcode or connected Apple device. Apple linkage, Swift tests, protection inheritance and physical memory/lock/Widget gates must be validated elsewhere; no achievement claimed. Actual native adapter can be built/tested on Linux first.
- Next action: publish core child PR, then native bridging and source/index publication. Full architecture is not yet completed. Device-only opt-in must not invoke background Widget legacy bootstrap; preserve normal mode until full parity.


## Current milestone: native transport and Swift client foundation

- Status: active; updated 2026-09-21T13:24:39.250694+00:00.
- Branch: `codex/ios-readindex-bridge`, base `7143b59` (core PR https://github.com/qiaoborui/beancount-ledger-web/pull/408). This milestone accompanies this note; all prior milestones pushed. Core PR408 open/mergeable, backend/gate passed; Vercel failure persists, not a successful preview.
- Completed: locked/cancellable gomobile bridge with private confined paths and bounded scalar responses; Python/C direct exclusive spool export (1KiB summary); runtime packaging; Swift actor export and injectable bounded client; explicit application SQLite link. See `docs/ios-readindex-bridge.md`. No workspace/session/UI default changes.
- Review: Go bridge independently reviewed without blockers; native/Swift review found missing application SQLite linkage, fixed in `App/LedgerMobile/project.yml`. Pinned Objective-C bindings generated successfully. Swift native calls require `LEDGER_BOUNDED_READ_INDEX` and otherwise fail unavailable.
- Validation: declared Go1.25.10 full backend tests/build PASS; targeted/race/vet PASS; Python3.14 runtime discovery 72 PASS including native C ABI and Go verifier interop; packaging seven PASS; shell syntax/diff checks PASS. Thirteen Swift tests added, NOT run (no Swift/Xcode). No Apple build or device protection/RSS evidence. Synthetic fixtures only.
- Files: `server/mobilereadindex/`; runtime C/header, new Python bridge/tests; Swift validator/client/tests and Package.swift; project.yml; native build/package scripts and packaging tests; bridge docs. Changes to be committed/pushed together before cross-machine handoff.
- Remaining in order: publish/check this child PR; coherent source/index publication and revision leases; opt-in bounded UI/Widget isolation; all reports/BQL/editor/import/Widget parity and protected spilling; failure/privacy/device scale gates before default switch.
- Blockers: Apple toolchain and physical device unavailable here. Do not invoke owner-operated signing/deployment workflows without authorization; do not claim all targets complete. Next concrete action: publish milestone then implement coherent publication foundation in a child branch.

## Publication foundation milestone

- Status active; updated 2026-09-21T14:00:54.265887+00:00. Branch `codex/ios-readindex-publication`, base `f20f655` (native transport PR https://github.com/qiaoborui/beancount-ledger-web/pull/409, pushed, mergeable, backend/gate green; Vercel not healthy).
- Added opt-in paired source/index manifest, protected preparation under workspace transaction lock, streamed source identity, immutable read leases, stale-source status and cancellation/privacy suppression. Reuses atomic pointer writer, retains last matched generation on failed rebuild. No financial commit/UI/default behavior switch. This selects already-confirmed sources, NOT atomic integration of future financial writes; that integration remains ahead.
- Added fake-backed publication failure/lifecycle/lease tests. Review fixed native absolute-path alias mismatch with fixed relative artifact names, strengthened last-pair late-failure tests and per-generation fake revisions. Static diff checks and native bridge regression pass; Swift tests remain unexecuted locally (no toolchain).
- Adds secret-free public macOS Swift package CI to exercise core and tests without private signing, deployment or ledger access. Apple framework link/protection/device gates remain separate; macOS package tests cannot satisfy them.
- Remaining: run/review Swift CI and fix failures, publish/check publication PR; opt-in bounded UI plus background/Widget isolation; integrate confirmed writes and all capability parity; protected spill and physical-device gates. Next action publish this milestone and inspect hosted Swift tests.


## Scoped publication CI compile fixes

- Task `20260921-ios-bounded-read-model`; compile-fix subtask completed; overall bounded read-model task remains active. Updated 2026-09-21T14:34:45.312971+00:00.
- Branch `codex/ios-readindex-publication`; code base `a259e3d30784c6297be14f25105f95383ba1df2d`; PR https://github.com/qiaoborui/beancount-ledger-web/pull/410. This note accompanies the scoped fix commit; user explicitly authorized commit/push.
- Only test changes: keep fixture FileManager statically typed as the existing lock-protected `PublicationFailureFileManager: @unchecked Sendable`; class-qualify static query/export helpers so closures do not capture XCTestCase via dynamic Self. All XCTest assertions unchanged; no production changes.
- Validation of this patch: `git diff --check` PASS; Swift 6.0.3 Linux container `swiftc -frontend -parse Tests/BoundedLedgerPublicationTests.swift` PASS with and without `-D LEDGER_BOUNDED_READ_INDEX`; `swift package dump-package` PASS; exact-change/assertion-preservation audit PASS. These are static checks, not macOS typechecking or execution.
- Next: push this commit and check PR mergeability and hosted macOS `swift test --jobs 2`; Apple runtime/device validation and prior broader milestones remain separate.

- Hosted validation update 2026-09-21T14:37:32.919221+00:00: fix commit `ffcb83b308bf997dff6fdfbc8e4ecb2e67fbb8be` pushed; macOS CI https://github.com/qiaoborui/beancount-ledger-web/actions/runs/35613133553 compiled successfully and all 21 `BoundedLedgerPublicationTests` passed. Full suite ran 445 tests, 15 skipped, with one out-of-scope failure at `LocalLedgerPresentationCacheTests.swift:38` (`testCachedBootstrapValidatesTheSnapshotTreeOnce`, observed 0 vs expected 1). Both requested compile errors are resolved; scoped subtask completed. PR410 open/mergeable. Next broader-task action: investigate that separate cache-test failure under separate authorization; no other test or production file changed.


## Scoped publication CI snapshot-validation fix

- Task `20260921-ios-bounded-read-model`; scoped fix active; overall architecture task remains active. Updated 2026-09-21T14:41:53.992448+00:00.
- Branch `codex/ios-readindex-publication`; code base `1c208678e9b58b533bf627cdd38aef09cf2c626f`; PR https://github.com/qiaoborui/beancount-ledger-web/pull/410. User authorized this minimal workspace/test fix, commit/push and hosted CI verification.
- Confirmed root cause: publication switched tree enumeration to bounded POSIX directory reads; the cache performance test's FileManager override no longer measured validation. Added a default no-op, Sendable observer at the full-tree validation entry (not recursive traversal), leaving traversal behavior unchanged. (Initializer compatibility correction recorded below.) Replaced obsolete FileManager instrumentation; assert exactly one validation for first cached snapshot, exactly two after a second snapshot read, and zero engine calls. The 1,000-file nested fixture and original once-per-snapshot assertion remain.
- Changed only `App/LedgerMobile/Sources/LocalLedgerWorkspace.swift`, `App/LedgerMobile/Tests/LocalLedgerPresentationCacheTests.swift`, and this shared note. No browser/UI, publication algorithm, traversal limits, ledger data or workflow changes.
- Host validation on patch: `git diff --check` PASS; Swift 6.0.3 Linux container compiler parse of both Swift files in default and `LEDGER_BOUNDED_READ_INDEX` modes PASS; `swift package dump-package` PASS. Source review confirms observer runs only at full-tree entry, existing validation unchanged. These are static checks, not Apple execution.
- Next: commit/push, watch macOS `iOS core tests` full package suite, fix any related failures, record tested SHA/run and inspect PR conflict state. No device or full architecture completion claimed.

- Hosted follow-up 2026-09-21T14:45:35.335421+00:00: first observer patch `237e2caad7f8f85d223c1d9f4fc0c2ab8f9cc00f` exposed an initializer compatibility error in [macOS run35613909408](https://github.com/qiaoborui/beancount-ledger-web/actions/runs/35613909408). Swift matches the existing unlabeled copy-hook trailing closure to the first eligible defaulted closure parameter; placing the observer before it broke `LocalLedgerWorkspaceTests.swift:338`. Moved observer after the existing copy hook without changing any other test. Swift6 strict host typecheck of an isolated initializer/call-site reproducer now PASS for existing throwing trailing closure, labeled copy/observer and default calls; default/bounded parser, manifest and diff checks PASS. Next push correction and rerun full hosted suite; first run is not a passing result.


## Snapshot-validation CI fix completed

- Task `20260921-ios-bounded-read-model`; scoped Swift CI fix **completed**, overall bounded read-model architecture remains active. Updated 2026-09-21T14:50:34.254639+00:00.
- Branch `codex/ios-readindex-publication`; pushed fix `237e2caad7f8f85d223c1d9f4fc0c2ab8f9cc00f` and initializer compatibility correction `ded0eb39b24542ae877a95a26550d232b8ec4ee1`. PR https://github.com/qiaoborui/beancount-ledger-web/pull/410 is open and MERGEABLE, with no conflicts.
- Hosted evidence at code revision `ded0eb39b24542ae877a95a26550d232b8ec4ee1`: [macOS15 Swift CI35614328362](https://github.com/qiaoborui/beancount-ledger-web/actions/runs/35614328362) **SUCCESS**, Apple Swift6.1.2, `swift test --jobs 2`: **445 tests, 15 skipped, zero failures**. Target cache test passed (1.271s); log reports first cached read snapshot scans1; second-read count2 and zero engine-call assertions pass. All21 publication tests pass. Host static/default-bounded parse/manifest and initializer compatibility typecheck pass.
- No weakening to expected0 or traversal/security changes. Final code scope remains workspace+cache test; no main browser worktree edits. Actual code and shared progress pushed; this evidence note accompanies a documentation-only follow-up.
- Separate general CI35614328357 failed an unrelated existing Go performance test (`TestGitHubWriteValidationLargeLedgerPerformance`: observed HTTP handler concurrency7 vs expected2..6); preceding general CI35613909402 passed. No backend changes made; do not conflate this with Swift suite health. Check next head CI before handoff.
- Next scoped action: push evidence note and verify final head checks/mergeability. No remaining Swift fix targets. Broader Apple native/device, UI/parity and architecture gates from earlier sections remain separate; no merge or architecture completion claimed.

## Isolated bounded browser milestone

- Status active; updated 2026-09-21T14:56:06.405637+00:00. User explicitly requested commit/push. Branch `codex/ios-bounded-browser`, code `d0129aa`, stacked on publication `dd2ce09` / PR410. Parent full macOS Swift suite now passes (445 tests, 15 skips, zero failures); previous failures fixed rather than waived.
- Implemented separate compile-time app shell with authentication, existing-local-ledger selection, explicit source revision confirmation/rebuild, one retained read scope and bounded request queue, replace-only 100-row pages and scalar detail. No LedgerSession/bootstrap/legacy fallback in experimental app path. Unsupported writes/reports/search/sync/Widget/import capabilities explicit. Full capability parity remains outstanding.
- Default Debug/Release unchanged; BoundedDebug/BoundedRelease flag inherited by all targets. Review corrected XcodeGen implicit default to Debug and blocked independent legacy account AppIntent entity resolution. Bounded widgets show unavailable state, timeline loader/network/background service/share import guarded. Old OS-retained Widget snapshots still require device upgrade validation.
- Ten model tests pass in isolated Linux Swift6 harness (Apple/Combine stubs, not full SDK). Default/bounded parser checks passed; static diff checks passed. Native UI Apple typecheck/linking/rendering, model tests against real Apple package, protection and device lifecycle remain unverified for this code. No private data/signing/deployment.
- Next: publish/check child PR and macOS CI; unsigned public Apple build validation; then capability/read-write parity and protected spilling, failure/device gates. All-target completion is NOT claimed.


## Public native simulator compile fixes

- Task20260921-ios-bounded-read-model; scoped native CI task active, overall architecture active; updated 2026-09-21T15:17:32.152880+00:00. Branch `codex/ios-bounded-browser`, code base `c791043`; PR https://github.com/qiaoborui/beancount-ledger-web/pull/411 (open, MERGEABLE). User authorizes scoped commit/push, public unsigned builds only.
- Real native run https://github.com/qiaoborui/beancount-ledger-web/actions/runs/35616699785 failed both configurations with Xcode26.3/iOS simulator26.2: BoundedDebug widget lacked Widget-required init(); Debug timed out typechecking the bookkeeping diff concatenation. Both native framework builds and mandatory Swift imports passed.
- Fixes: explicit default and kind widget initializers; split unchanged diff segments into independently inferred strings and join with the same three separators. No default capability, extension flag, financial-write or packaging behavior changes. Scope: two Swift files plus this note.
- Validation: workflow contract6 and private-IPA contract7 tests PASS; diff check PASS. Existing head macOS core run35616699456 SUCCESS:455 tests,15 skipped,0 failures. General CI35616699602 now SUCCESS. Native fixes require new hosted verification; not yet claimed passing.
- Next: push scoped fix, monitor both real native simulator jobs, fix any further concrete compile/link/package failures, then record final evidence and PR conflict status. Physical-device/runtime/parity gates remain outside this scoped task.

- Native follow-up 2026-09-21T15:28:53.426871+00:00: pushed `5851c75`; run35617948137 cleared both original errors but both configurations hit the same Swift6.2.4 compiler IRGen crash in SettingsView's actor-isolated LedgerLockInterval setter function conversion. Replace the bound method reference with an explicit forwarding closure (same synchronous session call); no toolchain downgrade or validation bypass. Core35617948389 and general CI35617948619 SUCCESS. Next push this narrow workaround and verify hosted builds again.

- Native follow-up 2026-09-21T15:40:12.544080+00:00: pushed `550438f`; run35619232929 cleared SettingsView IRGen and exposed the identical actor-function reabstraction crash for Date in TimeRangePicker. Apply the same forwarding closure to its start/end Binding setters (the only remaining direct method Binding setters found). No session/date behavior changes. Core35619232754 and general CI35619232807 SUCCESS; both native configurations still require rerun.


## Public native simulator validation completed

- Task `20260921-ios-bounded-read-model`; **scoped public native CI task completed**, overall bounded architecture remains active. Updated 2026-09-21T15:53:00.639155+00:00. Branch `codex/ios-bounded-browser`; tested/pushed code `67ae5166817842e38721a6ae67b7ba01c076744c`; fixes `5851c75`, `550438f`, `67ae516`. PR https://github.com/qiaoborui/beancount-ledger-web/pull/411 OPEN/MERGEABLE with no conflicts.
- Real public [native run35620520765](https://github.com/qiaoborui/beancount-ledger-web/actions/runs/35620520765) **SUCCESS**: Debug job106402134510 (15:50:25Z completion) and BoundedDebug job106402134397 (15:48:47Z completion), 2026-09-21. Xcode26.3, iOS simulator26.2, arm64. Both real framework builds, mandatory native imports, app/widget/share compilation and linkage, Python/resource packaging and all output existence gates passed. No stubs, weakened gates, private runner/ledger, signing credentials or production access. Existing resource phase performs public ad-hoc signing only.
- [macOS core35620520790](https://github.com/qiaoborui/beancount-ledger-web/actions/runs/35620520790) **SUCCESS**,455 tests/15 skipped/0 failures. [General CI35620520910](https://github.com/qiaoborui/beancount-ledger-web/actions/runs/35620520910) **SUCCESS attempt2**; attempt1 hit the known unrelated Go concurrency fixture (7 vs2..6). Reran failed jobs without editing backend/tests. Local workflow contract6/private-IPA contract7 and diff checks pass.
- Changed source scope: Widget-required initializer, split bookkeeping string inference without output changes, explicit equivalent Settings/time-range Binding setter closures avoiding Swift6.2.4 IRGen crashes. No workflow/build-script/link changes needed; Debug default and project-wide bounded isolation preserved.
- Vercel preview remains failed; PR mergeStateStatus UNSTABLE is not a native CI failure. No merge performed. Next: review PR411; physical-device execution/protection/lifecycle/old Widget snapshots, full feature parity and protected spilling are separate remaining architecture gates, not established by simulator builds. This evidence note accompanies a documentation-only follow-up commit.

## Detail record pagination milestone

- Active; updated 2026-09-21T16:41:43.838644+00:00. Branch `codex/ios-readindex-record-pages`, base `b9eeb90` / PR411. Parent real unsigned simulator Debug and BoundedDebug builds PASS, macOS455 tests/15 skips/0fail PASS; no physical-device proof.
- Added `DetailRecords` in native core/bridge: source-order exact records, default100/max500, 1MiB retained/encoded bounds, strict operation/revision/entry/sequence cursors. Existing whole-detail all-or-error API unchanged; no schema migration or temp sorts. Swift bounded client/publication/browser now traverses details with first/next replace-only pages, explicitly partial scalar display. No legacy default changes.
- Native independent review no blockers; Go1.25.10 targeted/race/vet/full uncached backend tests/build PASS under minimal env. Swift integration independently reviewed; 20 Linux stub-harness tests and both-mode parsing PASS. Actual Apple/full publication tests for this code await CI; earlier native results do not validate the new method. No end-to-end Swift/native paging or physical device run yet.
- Changed: new native detail page implementation/tests, bridge method/tests; bounded Swift client/publication/browser/models/tests; core docs. Next publish/check child PR, run Apple CI, then typed account/posting/price projection and accounts/balance capability slice. Reports/BQL/editor/import/Widget parity, coherent confirmed-write publication, protected spill and device acceptance remain outstanding.

## Schema2 account/native-balance milestone

- Active; updated 2026-09-21T18:02:40.061082+00:00. Branch `codex/ios-readindex-accounts`, base `33213d0` / PR412 (record paging pushed, mergeable, real native/package checks pass; general CI last observed failed, inspect live result rather than claiming green).
- Added exact typed posting/account-event/price projections with complete replay verification and inverse counts; schema2 requires explicit derived rebuild. Bounded account catalog and per-account native nominal currency balances, strict bound cursors/decimal arithmetic limits. Swift transport/publication/browser integrated with replace-only pages and explicit native-unit limitations. See `docs/ios-readindex-accounts.md`.
- Reviews fixed decimal fractional-scale false resource limits, Unicode canonical-equivalence account identity confusion, and duplicate openID acceptance. All preserved exact canonical data; no default UI or financial-write change.
- Validation: Go1.25.10 full tests/build, full serialized race, scoped vet, both100k tests PASS. Full vet has pre-existing app test goroutine Fatalf findings outside scope. Swift49 Linux stub-harness tests pass repeatedly; both-mode parse pass. Fresh Apple native/full publication CI pending; no physical-device validation.
- Next publish/check this child PR and real Apple tests; account activity/running balances and valuation compatibility next. Remaining reports/BQL/editor/import/Widget, confirmed-write coherent publication, protected spill, device failure/scale gates are not complete.

## PR413 macOS account-test CI fix

- Task `20260921-ios-bounded-read-model`; scoped code fix **completed**, all-green CI target **blocked** on unrelated fixture; broader architecture remains active. Updated 2026-09-22T02:40:35.482674+08:00. Branch `codex/ios-readindex-accounts`, PR https://github.com/qiaoborui/beancount-ledger-web/pull/413; tested/pushed code `f25367ca0078d94472eb8773781fa7dec5d4b325`. This follow-up changes documentation only.
- Confirmed initial core35635854785 fails at BoundedAccountsTests.swift:216 because Darwin prohibits semaphore.wait in async Task.detached. Moved only that wait into DispatchQueue.global().async via withCheckedContinuation, matching browserTestWait/detail synchronization. Five-second timeout and all assertions for both operations preserved; no production/backend changes.
- Hosted [macOS package35638635547](https://github.com/qiaoborui/beancount-ledger-web/actions/runs/35638635547) **PASS**, 481 tests/15 skipped/0 failures, including account lock/busy/cancel/close regression. [Native35638635565](https://github.com/qiaoborui/beancount-ledger-web/actions/runs/35638635565) **PASS** both Debug and BoundedDebug real unsigned simulator builds. No physical-device claim.
- [General CI35638635549](https://github.com/qiaoborui/beancount-ledger-web/actions/runs/35638635549) failed attempts1 and2 solely on pre-existing TestGitHubWriteValidationLargeLedgerPerformance (concurrency7, expected2..6), same as initial35635854667. Used the single authorized failed-jobs rerun; no unrelated fix or further rerun. Vercel also failed outside scope. PR OPEN/MERGEABLE, UNSTABLE; no merge.
- Local diff check PASS; hosted validation only, no local builds/downloads/worktree cleanup/main-worktree changes. Next monitor any checks triggered by this evidence-only update; if the fixture remains red, parent must handle it separately or authorize additional action.

## Account activity milestone

- Active; updated 2026-09-21T19:44:07.444823+00:00. Branch `codex/ios-readindex-account-activity`, code `90fa94c`, parent `0275d32` / PR413. Parent actual native Debug/BoundedDebug and package481/15skip/0fail pass; general backend CI repeatedly fails unrelated existing GitHub validation concurrency fixture (7 vs2..6), not waived/changed. Vercel also not green.
- Added native exact AccountSummary and transaction-grouped AccountActivity, ascending keysets with recomputed running prefixes, same schema2, bounded rows/bytes and strict filter/revision cursors. ExactDecimal-to-ExactDecimal addition fixes normalized-wire/raw-digit-budget false rejection (review regression at4096 fractional places). Swift client/publication/browser integrates currency selection, summaries, replacement activity pages and detail route; nominal units only, no valuation parity claim.
- Go targeted/race/vet/full tests/build/100k pass; P2 regression fixed with positive/negative/ranged/prefix tests. Swift62 harness tests and default/bounded parse pass; independent review no further blockers. Fresh real Apple suite/native validation pending. Rebase onto parent CI repair resolved only duplicate semaphore-test comment, preserving tested dispatch continuation.
- Next publish/check this child PR and Apple checks. Remaining valuation/report/BQL/write/import/Widget parity, protected spilling, coherent confirmed-write publication and device memory/privacy/failure gates remain active.

## CI concurrency instrumentation repair

- Active overall; updated 2026-09-22T03:20:00.106139+00:00. Branch `codex/ios-validation-concurrency-fixture`, base `9de3b94` / PR414. Parent Swift package and real default/experimental simulator checks pass, but backend/Gate failed the existing download-concurrency metric.
- Deterministically reproduced a fixture lifetime mismatch: six client requests can finish while seven server handlers overlap during cleanup. Test now counts client RoundTrip through underlying Body.Close; production six-worker limit unchanged. Retains every original timing/request/cache/concurrency/commit assertion. Added EOF/close/error/once/filter and deterministic handler7/client6 regressions.
- Go1.25.10 repeated targeted/race/full backend/build checks PASS; independent review repeated focused race with real bean-check PASS, no blockers. Negative control changing production cap to7 via temporary overlay correctly fails original bound; actual production file unchanged. Hosted CI pending publication.
- Next publish/check repair PR, then continue valuation/reports/BQL and remaining parity. Physical-device and protected-spill gates remain unfulfilled; do not treat CI repair as full plan completion.

## Latest valuation checkpoint

- Updated 2026-09-22T13:17:34.364329+08:00; broader task remains active. Current branch `codex/ios-readindex-valuation`, based on `7629d60` / PR #415. Go schema-3 valuation and Swift transport implementation are ready for a draft checkpoint; Apple SDK/full publication/native validation remains pending.
- Current resumable scope, validation limits and next steps: [20260922-ios-readindex-valuation](20260922-ios-readindex-valuation.md). Earlier milestone statuses above are historical.
