# 20260921-ios-bounded-read-model

- Status: active (architecture proposed; cross-agent continuation requested).
- Updated: 2026-09-21T17:55:58.237379+08:00.
- Goal: redesign mobile reads to avoid huge in-memory canonical JSON; user open to SQLite/DuckDB/no DB. Recommended direction is immutable SQLite-derived index, bounded streaming export and paginated/scoped read APIs. Canonical booking still has full AST memory; no constant-memory claim.
- Authorization: user asked to commit/push the current proposal for another agent. No new product implementation this turn; database choice/limits are recommendations, not measured achievements or blanket permission to drop compatibility.
- Branch: `codex/ios-typed-response`; application code revision covered: `dc24f3325acc27336f0cf4c3dbda035835b887cb`; PR https://github.com/qiaoborui/beancount-ledger-web/pull/406. Parent stack: PR404 baseline -> PR405 validation-only -> PR406 typed response. Read live PR state before resuming; no automatic merge requested.
- Deliverable: `docs/plans/20260921-ios-bounded-read-model.md`; this handoff. These are the only edits in this publication milestone. Publication commit accompanies this note; verify remote branch contains both files before cross-clone work. Earlier application code is already pushed; no SQLite implementation exists.
- Completed: reviewed current local/Web read paths, canonical exporter and runtime packaging; consulted official SQLite/DuckDB references; independent architecture critique incorporated; documented memory/data bounds, precision, atomic revision publication, privacy, failure handling, alternatives and phased delivery.
- Decisions/limits: .bean remains sole source of truth; derived DB must be disposable/rebuildable; no enormous JSON, hidden full-snapshot fallback or unbounded UI paging in new read mode; preview/confirmation canonical validation retained; no changes to history retention or production financial data. Native Python sqlite3 is currently pruned, so adapter/package availability must be verified, not assumed.
- Remaining steps in order: (1) fetch branch and compare notes/Git/PR state; (2) finalize native Go/SQLite adapter linkage, exact versioned stream/schema, existing BQL/report coverage and iOS protection/publication contract; confirm any capability reductions with user; (3) implement protected streaming exporter + canonical parity tooling in focused branch; (4) opt-in coherent index publication and paged reads; (5) cover full screen/query surface and pass crash/privacy/scale gates before default switch.
- Relevant files: `App/LedgerMobile/Runtime/ledger_validator.py`, `BeancountRuntime.c`, `App/LedgerMobile/Sources/LocalLedgerEngine.swift`, `EmbeddedBeancountValidator.swift`, `LocalLedgerRepository.swift`, `LocalLedgerWorkspace.swift`, `server/internal/app/local_transport.go`, `ledger_index_port.go`, `ledger_read_service.go`, `server/mobilecore/dispatch.go`, `scripts/build-beancount-ios-resources.sh`. Reuse accounting helpers but do not retain snapshot-shaped reads as the new storage interface.
- Validation: design-only; review for privacy/whitespace/paths. No new Go/Swift/web runtime tests run for this documentation commit. Prior code checks recorded in B1/B2 notes do not validate this proposed database architecture. Future commands: Python runtime unittest discovery; Swift portable and Release device/apphost build; Go full test/build if backend changes; synthetic generator1k/10k/100k and canonical golden parity plus bounded-read/crash/disk-full/lock/cancel/revision tests described in plan. Physical-device budgets unmeasured.
- Blockers/open decisions: actual mobile driver/linking, exact schema/export metadata types, BQL bounded compatibility and runtime budgets need completion before an implementation-ready specification. No credentials or external service needed for planned runtime. Original private data/local checkpoints are not part of the shared handoff; use synthetic ledgers.
- Next concrete action: fetch `origin/codex/ios-typed-response`, read the proposal, and turn the architecture into a decision-complete first implementation package without treating the entire migration as already approved/executed.
