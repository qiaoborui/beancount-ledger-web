# 20260921-ios-kernel-performance

- Status: active (measurement complete; publication in progress).
- Updated: 2026-09-21T16:02:38.287987+08:00
- Goal: explain the iOS local engine and establish current baseline before proposing stronger, evidence-based optimization targets. Previous proposed SLOs were rejected as too lenient and remain withdrawn.
- Authorized scope: source investigation, opt-in baseline infrastructure, synthetic scaling and isolated measurements. Original authorized source stays read-only; no private content or derived metrics are published. No product optimization, production deployment or physical-device installation authorized.
- Branch: `codex/ios-kernel-performance-plan`; product code revision measured: `1e3c9aa8ae3cd4bdce61017399bfbd6611608274`. Task edits not yet committed/pushed; no PR yet. Not ready in another clone until publication below is updated.
- Completed: architecture/plan document; Go transport vs JSON harness; simulator repository/validation/preview/confirm baseline; deterministic generator with canonical counts/totals verification; portable Swift test membership corrected while UI tests stay in Xcode; native dependencies rebuilt from source. Independent static review findings fixed (exclusive outputs, explicit failure metrics, cleanup/error handling, honest measurement labels).
- Findings safe to publish: explicit-include two-account CNY 10k simulator fixture completed; preview p50 2.25s, durable confirmation 1.20s, after-commit bootstrap 1.36s. 100k canonical exceeds 16 MiB bridge cap and correctly receives request.too_large: capacity failure, not successful latency support. Source wildcard includes exposed an existing write-side reader limitation; synthetic fixture corrected, product code unchanged.
- Changed files: `App/LedgerMobile/Package.swift`, `App/LedgerMobile/Tests/LocalLedgerPerformanceBaselineTests.swift`, `server/mobilecore/performance_baseline_test.go`, `scripts/generate-ios-performance-fixture.py`, `docs/ios-performance-baseline.md`, `docs/plans/20260921-ios-kernel-performance.md`, this handoff.
- Validation: full `go test ./...` passes with deployment environment removed; `go build ./cmd/ledger-web` passes. Portable `swift test` under Xcode developer directory: 403 tests, 13 skips, zero failures. Release simulator test build succeeds with ENABLE_TESTABILITY=YES; final opt-in apphost baseline passes including invalid-stage rollback. Generator 10k and host harness smoke pass; 100k canonical golden/capacity smoke passes. No web changes, no web checks needed.
- Failed attempts resolved: ambient deployment configuration made unrelated auth/Postgres default Go tests fail; rerun with toolchain-only environment passes. Default CommandLineTools Swift crashed loading a framework; Xcode toolchain succeeds. First synthetic wildcard preview rejected; explicit includes succeed. Do not include failed/exploratory measurements in completed baseline statistics.
- Safety: original source hash set verified unchanged; disposable simulator and copied sensitive artifacts/result bundles/logs removed. Only numeric metrics retained in local checkpoint storage, never Git. No production financial writes or remote integration tests.
- Not tested/claimed: physical iPhone/UI/thermal/peak memory, long soak, full bill import/Widget/concurrency fault matrix, 100k writes. Simulator phase samples are not calibrated device p95. Follow-on implementation packages and revised device SLOs still require approval.
- Remaining execution order: commit only task files; push focused branch and create PR with validation; check mergeability/conflicts/CI; update both handoffs to completed for baseline scope. Do not merge or deploy without approval.
- Next step after baseline delivery: approve a focused optimization package and identify the physical acceptance device. See `docs/ios-performance-baseline.md` for reproducible commands and `docs/plans/20260921-ios-kernel-performance.md` section 7 for public synthetic results.
