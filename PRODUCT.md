# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是管理自己账本的单人 Beancount 用户。产品部署在用户控制的环境中，用于每天快速检查财务状态，以及按月完成复盘、记账、导入、对账和账户维护。

## Product Purpose

Beancount Ledger Web 将私人 Beancount 账本转化为可浏览、可分析、可安全写入的本地优先财务工作台。成功意味着用户能先快速判断当前余额、变化与异常，再无缝下钻到分类、账户和具体流水，并在写入前完成预览与验证。

## Positioning

产品以 Beancount 文本账本和私有仓库作为事实源，在不牺牲可审计性、隐私和可迁移性的前提下，提供现代 Web 数据分析、离线缓存、人工确认写入和自托管能力。

## Operating Context

- 用户围绕月度时间范围查看收入、支出、结余、净资产、分类和账户变化。
- 交易、账户、商品、价格、预算和导入结果最终仍对应 Beancount 文件与术语。
- 日常路径通常从总览和异常判断开始，再进入看板、账户或流水进行核查。
- 产品可作为桌面浏览器中的高密度工作台，也可作为移动端 PWA 用于查看和快速记账。
- 金额与敏感资产需要通过密码、Passkey 或既有解锁机制后展示。

## Capabilities and Constraints

- 保留现有 Beancount 数据模型、账户路径、交易来源定位和安全写入流程。
- 保留隐私设置、敏感金额锁定、离线能力、移动导航、导入、对账、AI 记账草稿和账户管理功能。
- 最终写入必须继续经过预览、服务端校验、`bean-check` 与回滚保护。
- 桌面端需要高信息密度和紧凑指针控件；移动端需要结构性重排和足够触控尺寸，而不是等比例缩小桌面界面。
- 后端接口可在新信息架构确实缺少聚合数据时扩展，但不得复制已有解析或汇总逻辑。

## Brand Commitments

- 产品名称保持 Beancount Ledger Web；界面可简称 Ledger。
- 视觉世界必须现代、克制、数据优先，不延续类纸张、账本拟物或厚重卡片式仪表盘。
- 主要界面使用冷白、深墨与中性灰作为底色，并以单一深钴蓝承担导航、主操作、选中和焦点等功能强调；不使用棕色、铜色或无意义的彩色分类装饰。
- 红色只用于真实错误、风险、异常、破坏性操作或负向警报。
- 用户提供的参考图只作为桌面信息密度与数据工作台气质的方向证据，不得直接照抄品牌、构图或细节。
- 桌面当前页面标题不得在顶部导航和内容区重复出现。

## Evidence on Hand

- 真实功能与产品约束来自现有 React/Go 应用代码和仓库文档。
- `examples/preview-ledger/`、`examples/minimal-ledger/` 和 `examples/chinese-personal-ledger/` 提供可安全预览的数据。
- 用户提供了现代深色财务工作台截图作为桌面信息密度与数据工作台气质的参考。
- 没有可用于宣传的客户、收益、性能或市场证明；未来设计不得虚构这些内容。

## Product Principles

1. 先判断，再下钻：首屏必须优先回答余额、变化和异常。
2. 数据忠于账本：界面表达不得掩盖 Beancount 的账户、交易和来源事实。
3. 隐私默认可信：敏感信息的隐藏、解锁与重新锁定始终清晰可控。
4. 桌面为操作台，移动为随身工具：两种场景共享能力，但采用不同的信息结构。
5. 写入保持人工确认：任何自动化都不能绕过预览、验证和回滚边界。

## Accessibility & Inclusion

- 保持键盘导航、可见焦点、语义化控件与屏幕阅读器标签。
- 正常财务数据不能仅通过颜色区分；异常同时使用文字、图标或结构提示。
- 移动端核心操作保持约 44px 触控目标，桌面端使用紧凑但可辨识的指针控件。
